# Backend API base URL for local Vite proxy
VITE_API_BASE_URL=http://localhost:8080

# Vercel-only backend origin for production rewrites.
# Set this in Vercel Project Settings, not in frontend code.
# Must be HTTPS and must not include /api.
BACKEND_API_ORIGIN=https://api.example.com

# Google OAuth Client ID - Must be configured for each domain
# For localhost: Create credential in Google Cloud Console for http://localhost:3000
# For Vercel: Create credential for https://sep-490-fe-medical-ai-assistant.vercel.app
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
